const lib=require('./_provider-lib');
const notify=require('./_notify-lib');
const BUCKET='provider-applications';
async function cleanupPending(aid,pid,eid){if(!eid)return;try{const rows=await lib.sbJson(`/rest/v1/job_service_evidence?select=id,storage_path,status&id=eq.${encodeURIComponent(eid)}&assignment_id=eq.${encodeURIComponent(aid)}&provider_id=eq.${encodeURIComponent(pid)}&limit=1`),x=rows?.[0];if(!x||x.status!=='PENDING')return;await lib.sbJson(`/rest/v1/job_service_evidence?id=eq.${encodeURIComponent(eid)}`,{method:'DELETE',headers:{Prefer:'return=minimal'}});if(x.storage_path){const enc=String(x.storage_path).split('/').map(encodeURIComponent).join('/');await lib.sbFetch(`/storage/v1/object/${BUCKET}/${enc}`,{method:'DELETE'}).catch(()=>{});}}catch(_){}}
exports.handler=async event=>{if(event.httpMethod!=='POST')return lib.json(405,{error:'Method not allowed'});if(!lib.sameOrigin(event))return lib.json(403,{error:'Invalid request origin'});let auth=null,b=null,eid='';try{auth=await lib.requireProvider(event);b=JSON.parse(event.body||'{}');const action=String(b.action||'').toUpperCase(),payload=b.payload||{};if(!/^[0-9a-f-]{36}$/i.test(String(b.assignment_id||'')))return lib.json(400,{error:'Invalid assignment.'});if(['ARRIVE','COMPLETE'].includes(action)){eid=String(payload.evidence_id||'');if(!/^[0-9a-f-]{36}$/i.test(eid))return lib.json(400,{error:action==='ARRIVE'?'Arrival photo is required.':'Completion photo is required.'});const expected=action==='ARRIVE'?'ARRIVAL':'COMPLETION';const ev=await lib.sbJson(`/rest/v1/job_service_evidence?select=id,evidence_type,assignment_id,provider_id,status&id=eq.${encodeURIComponent(eid)}&assignment_id=eq.${encodeURIComponent(b.assignment_id)}&provider_id=eq.${encodeURIComponent(auth.provider.id)}&evidence_type=eq.${expected}&status=eq.PENDING&limit=1`);if(!ev?.[0])return lib.json(400,{error:'The required pending service photo could not be verified for this Provider.'});}const d=await lib.sbJson('/rest/v1/rpc/provider_live_service_action',{method:'POST',body:JSON.stringify({p_actor:auth.user.id,p_assignment_id:b.assignment_id,p_action:action,p_payload:payload})});
const ctx=await notify.assignmentContext(b.assignment_id).catch(()=>null),j=ctx?.jobs||{},notices=[];
const baseDetails=[['Job',j.reference],['Service',j.service_name],['Provider',ctx?.providers?.display_name||auth.provider.display_name],['Schedule',`${notify.formatDateTime(ctx?.scheduled_start)} → ${notify.formatDateTime(ctx?.scheduled_end)}`]];
let title='',intro='',customerIntro='';
if(action==='ARRIVE'){title='Provider arrived';intro=`${ctx?.providers?.display_name||auth.provider.display_name||'Provider'} recorded arrival with required evidence.`;customerIntro=`Hi ${j?.customers?.first_name||'there'}, your PLEASE professional has arrived.`;}
if(action==='START'){title='Service started';intro=`${ctx?.providers?.display_name||auth.provider.display_name||'Provider'} started the assigned work.`;customerIntro=`Hi ${j?.customers?.first_name||'there'}, your PLEASE service is now in progress.`;}
if(action==='COMPLETE'){title=d?.job_status==='COMPLETED'?'Service completed':'Provider completed assigned work';intro=`${ctx?.providers?.display_name||auth.provider.display_name||'Provider'} recorded completion with required evidence.`;customerIntro=d?.job_status==='COMPLETED'?`Hi ${j?.customers?.first_name||'there'}, your PLEASE service has been completed by the service team.`:`Hi ${j?.customers?.first_name||'there'}, one of the PLEASE professionals assigned to your service has completed their portion of the work.`;}
if(action==='REQUEST_EXTENSION'){title='Additional service time requested';intro=`${ctx?.providers?.display_name||auth.provider.display_name||'Provider'} requested additional service time.`;customerIntro=`Hi ${j?.customers?.first_name||'there'}, additional service time has been requested. Please review the request in Track Request.`;}
if(title){
  notices.push(await notify.sendAdmins({subject:`PLEASE — ${title} (${j.reference||'Job'})`,title,intro,details:baseDetails,message:action==='REQUEST_EXTENSION'?String(payload.reason||''):String(payload.note||''),ctaLabel:'Open Live Operations',ctaUrl:`${notify.baseUrl()}/admin-live-operations.html`,idempotencyKey:`please-admin-live-${b.assignment_id}-${action}-${d?.extension_request_id||''}`,replyToOverride:auth.user.email}));
  if(j?.customers?.email)notices.push(await notify.send({to:j.customers.email,subject:`PLEASE — ${title} (${j.reference||'Service'})`,title,intro:customerIntro,details:baseDetails.filter(x=>x[0]!=='Provider'),message:action==='REQUEST_EXTENSION'?`Requested additional time: ${Number(payload.extra_minutes||0)} minutes.${payload.reason?` Reason: ${payload.reason}`:''}`:'',ctaLabel:'Track Your Request',ctaUrl:`${notify.baseUrl()}/track-request.html`,idempotencyKey:`please-customer-live-${b.assignment_id}-${action}-${d?.extension_request_id||''}`}));
}
return lib.json(200,{...(d||{ok:true}),notifications_sent:notices.filter(x=>x?.sent).length});}catch(e){if(auth&&b&&eid)await cleanupPending(b.assignment_id,auth.provider.id,eid);console.error('provider-live-service-action',e);return lib.json(e.status||400,{error:e.message||'Live service action failed.'});}};

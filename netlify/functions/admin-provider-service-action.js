const lib=require('./_admin-lib');exports.handler=async event=>{if(event.httpMethod!=='POST')return lib.json(405,{error:'Method not allowed'});if(!lib.sameOrigin(event))return lib.json(403,{error:'Invalid request origin'});try{const a=await lib.requireAdmin(event),b=JSON.parse(event.body||'{}'),pid=String(b.provider_id||''),ids=Array.isArray(b.service_ids)?b.service_ids:[];if(!/^[0-9a-f-]{36}$/i.test(pid))return lib.json(400,{error:'Invalid provider id'});const d=await lib.sbJson('/rest/v1/rpc/admin_provider_service_action',{method:'POST',body:JSON.stringify({p_actor:a.user.id,p_provider_id:pid,p_service_ids:ids})});return lib.json(200,{ok:true,result:d});}catch(e){console.error('admin-provider-service-action',e);return lib.json(e.status||400,{error:e.status===401?'Unauthorized':(e.message||'Authorized services could not be updated.')});}};

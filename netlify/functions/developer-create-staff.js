const lib=require('./_admin-lib');exports.handler=async event=>{if(event.httpMethod!=='POST')return lib.json(405,{error:'Method not allowed'});if(!lib.sameOrigin(event))return lib.json(403,{error:'Invalid request origin'});try{const a=await lib.requireDeveloper(event),b=JSON.parse(event.body||'{}');const d=await lib.sbJson('/rest/v1/rpc/developer_create_please_staff',{method:'POST',body:JSON.stringify({p_actor:a.user.id,p_display_name:String(b.display_name||''),p_email:String(b.email||''),p_phone:String(b.phone||''),p_password:String(b.password||'')})});return lib.json(200,d||{ok:true});}catch(e){console.error('developer-create-staff',e);return lib.json(e.status||400,{error:e.message||'Staff account could not be created.'});}};

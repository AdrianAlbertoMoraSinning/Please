const lib=require('./_provider-lib');exports.handler=async event=>{if(event.httpMethod!=='POST')return lib.json(405,{error:'Method not allowed'});if(!lib.sameOrigin(event))return lib.json(403,{error:'Invalid request origin'});try{const a=await lib.requireProvider(event),b=JSON.parse(event.body||'{}');await lib.sbJson('/rest/v1/rpc/provider_portal_change_password',{method:'POST',body:JSON.stringify({p_actor:a.user.id,p_current_password:String(b.current_password||''),p_new_password:String(b.new_password||'')})});return lib.json(200,{ok:true},{'Set-Cookie':lib.clearCookie()});}catch(e){return lib.json(e.status||400,{error:e.message||'Unable to change password.'});}};

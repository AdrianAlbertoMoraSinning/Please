const lib=require('./_provider-lib');exports.handler=async event=>{if(event.httpMethod!=='POST')return lib.json(405,{error:'Method not allowed'});if(!lib.sameOrigin(event))return lib.json(403,{error:'Invalid request origin'});function quarter(v){return !v||/^\d{2}:(00|15|30|45)(?::00)?$/.test(String(v));}
try{const a=await lib.requireProvider(event),b=JSON.parse(event.body||'{}');const payload=b.payload||{};if(String(b.action||'').toUpperCase()==='SAVE_WEEKLY'&&Array.isArray(payload.availability)&&payload.availability.some(x=>!quarter(x.start_time)||!quarter(x.end_time)))return lib.json(400,{error:'Availability must use 15-minute increments.'});if(String(b.action||'').toUpperCase()==='ADD_EXCEPTION'&&(!quarter(payload.start_time)||!quarter(payload.end_time)))return lib.json(400,{error:'Availability must use 15-minute increments.'});const d=await lib.sbJson('/rest/v1/rpc/provider_portal_availability_action',{method:'POST',body:JSON.stringify({p_actor:a.user.id,p_action:String(b.action||''),p_payload:b.payload||{}})});return lib.json(200,d||{ok:true});}catch(e){return lib.json(e.status||400,{error:e.message||'Availability could not be updated.'});}};

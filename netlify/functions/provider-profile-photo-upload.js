const lib=require('./_provider-lib');const security=require('./_security-lib');const {randomUUID}=require('crypto');
const BUCKET='provider-applications',MAX=4*1024*1024,ALLOWED={'image/jpeg':'jpg','image/png':'png','image/webp':'webp'};
function magic(b,t){if(t==='image/jpeg')return b.length>3&&b[0]===0xff&&b[1]===0xd8&&b[2]===0xff;if(t==='image/png')return b.length>=8&&b.subarray(0,8).equals(Buffer.from([0x89,0x50,0x4e,0x47,0x0d,0x0a,0x1a,0x0a]));if(t==='image/webp')return b.length>=12&&b.subarray(0,4).toString('ascii')==='RIFF'&&b.subarray(8,12).toString('ascii')==='WEBP';return false;}
exports.handler=async event=>{if(event.httpMethod!=='POST')return lib.json(405,{error:'Method not allowed'});if(!lib.sameOrigin(event))return lib.json(403,{error:'Invalid request origin'});try{const a=await lib.requireProvider(event),ct=String(event.headers['content-type']||'').split(';')[0].toLowerCase();if(!ALLOWED[ct])return lib.json(415,{error:'Use JPG, PNG or WEBP.'});const b=Buffer.from(event.body||'',event.isBase64Encoded?'base64':'binary');if(!b.length||b.length>MAX||!magic(b,ct))return lib.json(415,{error:'Invalid or oversized photo.'});let originalName=String(event.headers['x-file-name']||event.headers['X-File-Name']||'profile-photo').trim();try{originalName=decodeURIComponent(originalName);}catch{}originalName=originalName.replace(/[\r\n]/g,' ').slice(0,180);const path=`providers/${a.provider.id}/profile/${randomUUID()}.${ALLOWED[ct]}`,enc=path.split('/').map(encodeURIComponent).join('/');const up=await lib.sbFetch(`/storage/v1/object/${BUCKET}/${enc}`,{method:'POST',headers:{'content-type':ct,'x-upsert':'false'},body:b});if(!up.ok)throw new Error(await up.text()||'Could not store profile photo.');const patch={profile_image_path:path,profile_image_name:originalName,updated_at:new Date().toISOString()};try{await lib.sbJson(`/rest/v1/providers?id=eq.${encodeURIComponent(a.provider.id)}`,{method:'PATCH',headers:{Prefer:'return=minimal'},body:JSON.stringify(patch)});}catch(e){if(!/profile_image_name/i.test(String(e.message||'')))throw e;delete patch.profile_image_name;await lib.sbJson(`/rest/v1/providers?id=eq.${encodeURIComponent(a.provider.id)}`,{method:'PATCH',headers:{Prefer:'return=minimal'},body:JSON.stringify(patch)});}return lib.json(200,{ok:true,file_name:originalName});}catch(e){console.error('provider-profile-photo-upload',e);return lib.json(e.status||500,{error:e.message||'Profile photo upload failed.'});}};